interface:
  display_name: "跨境吴老师周数据分析Skill"
  short_description: "父商品周度异动、高优先级拆分、30天缓慢趋势诊断、证据缺口闸门、BI生成与权限邮件分发"
  default_prompt: "Use $kuajing-wulaoshi-weekly-analysis to run this week's parent-ASIN single-period and 30-day dual-window trend anomaly analysis, classify current and slow high-priority anomalies, generate BI dashboards, apply the core-diagnosis/evidence-gap send gate, and prepare high-priority diagnosis outputs in trial mode."

dependencies:
  tools:
    - type: "mcp"
      value: "LingXing-MCP"
      description: "Technical data-source MCP used internally for authorized business-data retrieval"
      transport: "streamable_http"
      url: "https://openmcp.lingxing.com/mcp-servers/lingxing-mcp"

policy:
  allow_implicit_invocation: true
